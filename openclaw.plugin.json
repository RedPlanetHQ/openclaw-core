{
	"id": "openclaw-corebrain",
	"kind": "memory",
	"uiHints": {
		"apiKey": {
			"label": "Core API Key",
			"sensitive": true,
			"placeholder": "rc_pat_...",
			"help": "Your API key from app.getcore.me (or use ${CORE_API_KEY})"
		},
		"source": {
			"label": "Source",
			"placeholder": "openclaw_local",
			"help": "Memory namespace. Default: openclaw_{hostname}. All channels share this.",
			"advanced": true
		},
		"autoRecall": {
			"label": "Auto-Recall",
			"help": "Inject relevant memories + user profile before every AI turn"
		},
		"autoCapture": {
			"label": "Auto-Capture",
			"help": "Automatically store important information from conversations"
		},
		"captureMode": {
			"label": "Capture Mode",
			"help": "'all' (default) = filter out short texts and injected context",
			"advanced": true
		},
		"debug": {
			"label": "Debug Logging",
			"help": "Enable verbose debug logs for API calls and responses",
			"advanced": true
		}
	},
	"configSchema": {
		"type": "object",
		"additionalProperties": false,
		"properties": {
			"apiKey": { "type": "string" },
			"containerTag": { "type": "string" },
			"autoRecall": { "type": "boolean" },
			"autoCapture": { "type": "boolean" },
			"captureMode": { "type": "string", "enum": [ "all" ] },
			"debug": { "type": "boolean" }
		},
		"required": [ ]
	}
}
